#include <iostream>
#include "threadpool/ThreadPool.h"

/*void fn() {
    std::cout << "Thread1\n";
}

void fn2() {
    std::cout << "Thread2\n";
}

void fn3() {
    std::cout << "Thread3\n";
}

void fn4() {
    std::cout << "Thread4\n";
}

int main() {
    int const cnt_tasks = 100;
    ThreadPool threadPool(4);

    for (int i = 0; i < cnt_tasks; ++i) {
        threadPool.execute(fn);
        threadPool.execute(fn2);
        threadPool.execute(fn3);
        threadPool.execute(fn4);
    }

    return 0;
}
*/
